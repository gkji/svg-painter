export enum Drawing {
    init = 'init',
    start = 'start',
    moving = 'moving',
    end = 'end'
  }

export enum GraphType {
    line = 'line',
    rect = 'rect',
    round = 'round',
    path = 'path',
}
